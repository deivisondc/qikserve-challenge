import { Template } from "./Template";

const NotFound = () => {
  return (
    <Template
      title="Not Found"
      description="Could not find requested resource"
    />
  );
};

export { NotFound };
